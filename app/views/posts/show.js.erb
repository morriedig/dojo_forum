$("#all_replies").html("<%=j render :partial => 'replies/all_replies', locals: { replies: @replies } %>")
$('.pagination').html('<%= escape_javascript(paginate(@replies, theme: "Dojo", :remote=>true).to_s)%>');